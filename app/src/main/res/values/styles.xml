<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ResultCardViewStyle" parent="Theme.AppCompat.Light">
        <item name="cardCornerRadius">2dp</item>
        <item name="cardElevation">2dp</item>
        <item name="contentPaddingBottom">24dp</item>
        <item name="contentPaddingTop">24dp</item>
        <item name="contentPaddingLeft">16dp</item>
        <item name="contentPaddingRight">16dp</item>
        <item name="cardBackgroundColor">@color/cardBG</item>
        <item name="android:layout_marginBottom">15dp</item>
        <item name="android:layout_width">300dp</item>
        <item name="android:layout_height">200dp</item>
    </style>

    <style name="YesNoBtnStyle">
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <!--Override default snackbar-->
    <style name="TextAppearance.Design.Snackbar.Message" parent="android:TextAppearance" tools:override="true">
        <item name="android:textColor">@color/background</item>
    </style>

    <style name="stateWin">
        <item name="android:background">@color/colorWin</item>
        <item name="android:color">#fff</item>
    </style>

    <style name="stateDefeat">
        <item name="android:background">@color/colorDefeat</item>
    </style>

    <style name="stateDraw">
        <item name="android:background">@color/colorDraw</item>
    </style>

    <style name="stateWaiting">
        <item name="android:background">@color/colorWaiting</item>
    </style>

</resources>
