<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pinkertone.pinkercardsmvp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RatingActivity" />
        <activity android:name=".InDevelopingActivity" />
        <activity android:name=".FlashcardsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EnemiesActivity" />
        <activity android:name=".BattleActivity" />
        <activity
            android:name=".BattlesActivity"
            android:label="@string/title_activity_battles"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BattleResultsActivity" />
        <activity android:name=".signUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>